# Set real values in Jenkins UI after first startup.
# Manage Jenkins → Credentials → Global
credentials:
  system:
    domainCredentials:
      - credentials:

          - usernamePassword:
              scope: GLOBAL
              id: "USERNAME"
              username: "${APP_USERNAME}"
              password: "${APP_PASSWORD}"
              description: "App login credentials"

          - string:
              scope: GLOBAL
              id: "BROWSERSTACK_USERNAME"
              secret: "${BROWSERSTACK_USERNAME}"
              description: "BrowserStack username"

          - string:
              scope: GLOBAL
              id: "BROWSERSTACK_ACCESS_KEY"
              secret: "${BROWSERSTACK_ACCESS_KEY}"
              description: "BrowserStack access key"

          - string:
              scope: GLOBAL
              id: "BROWSERSTACK_APP"
              secret: "${BROWSERSTACK_APP}"
              description: "BrowserStack app id"
