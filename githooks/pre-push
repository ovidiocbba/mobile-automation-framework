#!/bin/sh

echo "==> Running pre-push checks..."

./gradlew preCommitCheck
RESULT=$?

if [ $RESULT -ne 0 ]; then
  echo "[ERROR] Push blocked: quality checks failed."
  exit 1
fi

echo "[OK] Push allowed: all checks passed."
exit 0
