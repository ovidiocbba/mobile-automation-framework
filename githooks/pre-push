#!/bin/sh

echo $'\n==> Running pre-push full verification...'

if ! ./gradlew -q check; then
  echo "[ERROR] Push blocked: tests, javadoc or quality checks failed."
  exit 1
fi

echo "[OK] Push allowed: full verification passed."
exit 0
